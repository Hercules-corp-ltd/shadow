{
  "name": "shadow-mail-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --env dev --local --port 8787",
    "typecheck": "tsc --noEmit",
    "schema:local": "wrangler d1 execute shadow-mail --local --file schema.sql",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260816.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.123.0"
  }
}
